# screen-reader-demo

## How to use demo:

There are two branches in this demo, one with semantic HTML and one without. 

`main` branch: best practices, semantic HTML
`bad-practices` branch: bad practices, divs only

To open the html file, open the folder screen-reader-demo on your file system and double click the index.html file.
Refresh browser to see changes reflected.